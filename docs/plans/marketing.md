
Count how many times the system has been fuzzy tested into a file,
use that in the github repo
open source the counter program + the count file
Make this part of the test cli so other apps can market their testing
build a coverage for the cli that shows you the coverage and includes branching
