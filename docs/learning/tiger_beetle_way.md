# Auditor (oracle pattern, model-based testing or differential testing)
if two independant implementations agree on every input/output for millions of random inputs, they're probably both correct
its how tiger beetle gets away with aggressive implementations even 
