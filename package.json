{
  "private": true,
  "scripts": {
    "build": "./zig/zig build codegen && ./zig/zig build scan -- ts/handlers/ --manifest=generated/manifest.json && tsx adapters/typescript.ts generated/manifest.json generated/dispatch.generated.ts",
    "sidecar": "tsx generated/dispatch.generated.ts /tmp/tiger-web.sock",
    "server": "./zig/zig build run -- --sidecar=/tmp/tiger-web.sock",
    "dev": "npm run build && concurrently 'npm run sidecar' 'sleep 1 && npm run server'"
  },
  "devDependencies": {
    "tsx": "^4",
    "concurrently": "^9",
    "typescript": "^5"
  }
}
