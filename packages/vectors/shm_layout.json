{
  "description": "SHM region memory layout — offsets for cross-language implementation",
  "region_header": {
    "size": 64,
    "fields": {
      "epoch":      { "offset": 0,  "size": 4, "type": "u32 LE", "description": "Bumped after every CALL write. Sidecar futex-waits on this address." },
      "slot_count": { "offset": 4,  "size": 2, "type": "u16 LE", "description": "Number of slot pairs in region." },
      "_reserved":  { "offset": 6,  "size": 2, "type": "u16 LE" },
      "frame_max":  { "offset": 8,  "size": 4, "type": "u32 LE", "description": "Maximum frame payload size (bytes)." },
      "_pad":       { "offset": 12, "size": 52 }
    }
  },
  "slot_header": {
    "size": 64,
    "fields": {
      "server_seq":   { "offset": 0,  "size": 4, "type": "u32 LE", "description": "Server increments after writing CALL to request area." },
      "sidecar_seq":  { "offset": 4,  "size": 4, "type": "u32 LE", "description": "Sidecar increments after writing RESULT to response area." },
      "request_len":  { "offset": 8,  "size": 4, "type": "u32 LE", "description": "Byte length of CALL frame in request area." },
      "response_len": { "offset": 12, "size": 4, "type": "u32 LE", "description": "Byte length of RESULT frame in response area." },
      "request_crc":  { "offset": 16, "size": 4, "type": "u32 LE", "description": "CRC32 of (request_len as LE bytes ++ request payload)." },
      "response_crc": { "offset": 20, "size": 4, "type": "u32 LE", "description": "CRC32 of (response_len as LE bytes ++ response payload)." },
      "_pad":         { "offset": 24, "size": 40 }
    }
  },
  "slot_pair": {
    "description": "slot_header + request_area + response_area",
    "size_formula": "64 + frame_max + frame_max",
    "request_area_offset": 64,
    "response_area_offset": "64 + frame_max"
  },
  "region": {
    "description": "region_header + slot_pairs",
    "total_size_formula": "64 + slot_count * (64 + frame_max * 2)",
    "first_slot_offset": 64,
    "slot_n_offset_formula": "64 + n * (64 + frame_max * 2)"
  },
  "crc_convention": {
    "description": "CRC32 is computed over: u32 LE payload_len bytes ++ payload bytes",
    "example": "For a 11-byte payload: crc32([0x0B, 0x00, 0x00, 0x00] ++ payload[0..11])"
  },
  "coordination": {
    "server_to_sidecar": "Server writes request area + request_len + request_crc, then increments server_seq, then futex_wake(epoch)",
    "sidecar_to_server": "Sidecar writes response area + response_len + response_crc, then increments sidecar_seq, then futex_wake(sidecar_seq address)"
  }
}
